{
  "name": "frenzai",
  "version": "1.0.0",
  "description": "Offline voice cloning and text-to-speech desktop app",
  "main": "electron/main.js",
  "author": "VoiceForge",
  "license": "MIT",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python -m uvicorn app.main:app --host 127.0.0.1 --port 8111",
    "dev:electron": "npx electron .",
    "build:frontend": "cd frontend && npm run build",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "electron": "^41.0.4",
    "electron-builder": "^24.13.0"
  }
}
